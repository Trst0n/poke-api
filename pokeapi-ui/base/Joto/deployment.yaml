apiVersion: apps/v1
kind: Deployment
metadata:
  name: joto-ui
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: joto-ui
  template:
    metadata:
      labels:
        app: joto-ui
    spec:
      containers:
        - image: pokeapi:0.1
          name: joto-ui
          ports:
            - containerPort: 3000
          env:
            - name : API_URL
              valueFrom:
                configMapKeyRef:
                  key: API_URL
                  name: pokeapi-cm
